namespace Siasm
{
    /// <summary>
    /// マッチ時のリールタイプ
    /// jsonデータで保存する際に値が変わらないように明示的に値を指定
    /// </summary>
    public enum CardReelType
    {
        None = 0,
        Attack = 1,
        Guard = 2
    }
}
